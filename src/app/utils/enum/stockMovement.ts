/**
 * Stock movement type enumeration
 */
export enum StockMovementType {
  IN = "IN", // Stock added (purchase)
  OUT = "OUT", // Stock removed (sale)
  ADJUSTMENT = "ADJUSTMENT", // Manual adjustment
}
